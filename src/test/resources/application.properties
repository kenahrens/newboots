# Test configuration - use H2 in-memory database
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate configuration for tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Disable MongoDB for tests
spring.data.mongodb.uri=mongodb://localhost:27017/test
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.mongodb.MongoDataAutoConfiguration

# gRPC server port for tests
grpc.server.port=0
