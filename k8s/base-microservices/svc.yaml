apiVersion: v1
kind: Service
metadata:
  name: newboots
  labels:
    app: newboots
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: newboots 